fix: clear all documented technical debt (7 items)

engine.py — stale backend detection now uses model-ID snapshots
  instead of comparing shared config object references (always false).
  Also fixed f-string logger to lazy %s format.

downloader.py — subprocess death now emits download_failed signal
  with exit code instead of silently returning (UI no longer hangs).

epochs.py — store original tmin from MNE data and use it in get_mne()
  instead of hardcoded 0.0. Documented in-place event ID mutation in
  docstring (intentional unification across files).

facade.py — attach_labels uses sfreq-based onset spacing (1 event/sec)
  instead of sequential sample indices (0,1,2...). Fixed f-string logger.

tools/__init__.py — real tool imports deferred to get_all_tools(mode="real")
  via _build_real_tools(). AVAILABLE_TOOLS uses module __getattr__ for
  lazy access. Mock tools still imported eagerly (lightweight).

config.py — load_dotenv() called before cls() in load_from_file() so
  field defaults (api_key, base_url, gemini_api_key, inference_mode)
  can read .env variables correctly.

retriever.py + indexer.py — RAGIndexer accepts optional client/embeddings
  parameters. Retriever passes its existing client to avoid opening a
  second QdrantClient to the same disk path (fixes potential lock issues).
  Indexer.close() only closes self-owned clients.

3986 passed | Ruff 0
