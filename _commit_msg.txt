audit: full codebase review — fix 3 P0, 11 P1, 4 P2 across 15 source files

P0 (crash) fixes:
- training_manager.py: move check_data() after super().__init__() to avoid
  QMessageBox on uninitialized QDialog
- model_summary.py: same pattern — check_data() after super().__init__()
- rag/indexer.py: add ImportError guard when langchain/qdrant not installed

P1 (logic bug) fixes:
- controller.py: guard _on_generation_finished with is_processing check to
  prevent stale callbacks after stop_generation
- controller.py: coerce None result to str in _handle_tool_result_logic
- controller.py: resume remaining HITL commands after confirmed execution
  instead of silently dropping them
- verifier.py: return VerificationResult instead of raising ValueError on
  malformed tool calls (caller expects return, not exception)
- api.py: guard against empty chunk.choices in streaming response
- run.py: --model CLI override is now transient (app.setProperty) instead
  of permanently writing to settings.json
- main_window.py: guard debug tool result against None before 'in' check
- evaluation/panel.py: use next(..., None) to prevent StopIteration crash
- tool_debug_mode.py: validate call_data type and 'tool' key presence
- tool_executor.py: remove double "Error:" prefix in error messages
- agent_manager.py: delegate switch_panel to main_window.switch_page()
  to properly uncheck nav buttons and call update_panel()

P2 (code quality) fixes:
- agent_manager.py: remove corrupted '??' characters in error message
- info_panel.py: guard highpass/lowpass format against None values
- observer_bridge.py: disconnect previous wrapper before reconnecting
  to prevent signal leak on repeated connect_to() calls
- run.py: use lazy %s logger formatting instead of f-string

Test updates:
- test_controller.py/cov: set is_processing=True for broken_json test
- test_verification_layer.py: expect VerificationResult instead of ValueError
- test_agent_manager_switch.py: add switch_page to MockMainWindow
- test_ui_misc.py: mock switch_page instead of stack.setCurrentIndex

Quality: Ruff 0 | 3986 passed | 17 skipped | 1 xfailed
