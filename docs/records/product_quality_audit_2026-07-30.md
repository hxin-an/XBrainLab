# Product Quality Audit - 2026-07-30

This record is the evidence ledger for the product-quality closure goal. It is
not a replacement for `docs/current.md`; current truth must be copied back to
the canonical documents after each validated slice.

Baseline:

- branch: `ux/assistant-product-v1`
- commit: `3869aaef73acf3fb30ce95d15868c2abcf17c6f5`
- baseline worktree: clean and pushed
- protected local dirty exception: unstaged repo-root `settings.json` only;
  `.vscode/settings.json` must remain untouched and clean
- excluded parallel work: dirty `stabilize/windows-public-beta` worktree

Status meanings:

- `open`: reproduced or source-verified and not fixed
- `in progress`: owned by the active closure goal
- `verified`: fixed and protected by focused plus regression evidence
- `external acceptance`: automated closure is complete but Windows human
  acceptance remains

## Architecture

| ID | Severity | Finding and evidence | Required closure | Status |
| --- | --- | --- | --- | --- |
| ARCH-01 | P0 | `QtObserverBridge._dispatch()` reports success without checking whether the consumer rendered the publication. `AgentManager` also drops the renderer's false result. A terminal training event can therefore be released before the corresponding revision is visible. | Acknowledgement-requiring consumers return an explicit boolean; only `True` acknowledges the revision. Protect no-render, delayed-render and exactly-once terminal delivery. | verified across checkpoints `ae441050` and `7c0237d9`: only an explicit `True` acknowledges delivery; no-render publications remain pending and bounded retry/coalescing preserves exactly-once terminal delivery; main-agent focused plus adjacent validation passed 156 tests |
| ARCH-02 | P1 | Application code still constructs controller adapters through `Study.get_controller()`, so the nominal application layer depends on an outer UI/controller layer. | Move one command family at a time to manager/domain ports. Controllers become outer adapters only. Add an import/source guard. | verified in the working checkpoint: application composition uses Study-owned manager/domain services and typed runtime ports for Dataset, Preprocess, Training and Analysis; application source guards reject controller imports, adapter symbols and `Study.get_controller()` reintroduction. The main-agent architecture gate passes 264 unit checks plus the repository compliance runner. Exact-commit closure remains under FUNC-06. |
| ARCH-03 | P1 | Product `CommandResult.runtime` / `local_payload` can expose live Raw, Epoch or generator objects across application and Qt/thread boundaries. | Replace product object payloads with immutable detached query/render DTOs. Remove product `include_objects` paths. | verified in the working checkpoint: product `CommandResult` and read commands physically remove runtime/live-object opt-ins; Evaluation and training-history consumers use generation-bound detached identities/copies; strict JSON projection rejects non-finite values. The remaining `local_result_payload` compatibility helper and its architecture allowlist were also removed, and the human-like walkthrough now accepts detached diagnostics only. The main-agent re-gate passes the 200-test walkthrough harness, focused mutable-boundary guards, 1,418 backend/application plus architecture tests and 17 external-label, epoch and public cross-source integrations. Exact-SHA evidence remains under FUNC-06. |
| ARCH-04 | P1 | State-changing UI refresh can arrive through command-result refresh, controller observers and revisioned application publication. Suppression ledgers mask multiple truths. | Make revisioned application publication the single state-changing refresh truth. Keep progress as transient events only. | verified in the working checkpoint: product panels render state from revisioned application publication; Training command results no longer reconcile terminal state or mutate Start/Stop/readiness, while progress remains a transient event. Ordering regressions cover result-before-publication, publication-before-result and duplicate late results; a call-chain source guard blocks reintroduction. Main-agent focused and adjacent re-gate passes 441 tests plus the repository architecture runner. Exact-commit closure remains under FUNC-06. |
| ARCH-05 | P1 | Product panels still require broad controller bundles instead of narrow query/publication/action ports. | Convert panels incrementally and keep compatibility only outside the product path. | verified in the working checkpoint: MainWindow materializes all five product panels through narrow ports; Training receives query, publication, action and transient-progress ports and discards compatibility controllers in typed-port mode. Standalone/no-runtime constructors retain explicit compatibility branches as P2 cleanup, not product wiring. Exact-commit closure remains under FUNC-06. |
| ARCH-06 | P2 | `ApplicationService`, `LLMController`, `AgentManager`, Data Import and Dataset actions are large responsibility aggregators. | Extract lifecycle, dispatch, runtime and presentation slices behind behavioral tests; do not refactor by line count alone. | verified in the working checkpoint: shutdown fencing/reconciliation, immutable Assistant application publication, external-label import state and recipe reload now have focused owners instead of writable host mirrors. Source guards reject private shutdown aliases and coordinator-state round trips; the main-agent regression passed 491 application/runtime/publication/architecture tests, and an independent architecture re-review returned PASS after the construction-race and runtime-alias guards were added. Exact-commit closure remains under FUNC-06. |
| ARCH-07 | P1 | Legacy load/label commands remain available in the default automation catalog. | Exclude legacy commands by default; compatibility requires an explicit opt-in and cannot become readiness fallback. | verified at checkpoint `9068a5d3`: default automation excludes legacy commands and compatibility requires explicit opt-in |
| ARCH-08 | P1 | Some readiness/capability paths can fall back when application publication is absent. | Real Study product contexts fail closed when publication or capability truth is missing. | verified in the working checkpoint: all product panel readiness/action paths require usable application publication and typed capabilities; Training configuration, split, start, stop and history actions fail closed when publication/capability truth is absent or malformed. Compatibility helpers are blocked before product contexts. Exact-commit closure remains under FUNC-06. |

## Security, Privacy and Resources

| ID | Severity | Finding and evidence | Required closure | Status |
| --- | --- | --- | --- | --- |
| SEC-01 | P0 | Training/evaluation persistence used `torch.load(..., weights_only=False)` before validating the loaded type. | Introduce a versioned safe artifact format: JSON manifest/scalars, NPZ numeric arrays with `allow_pickle=False`, and `weights_only=True` state dicts. Product rejects legacy unsafe records with a clear migration boundary. | verified: versioned JSON/NPZ store, SHA-256 integrity, tensor-only checkpoints and explicit unsafe-legacy rejection; 157 record tests plus two pipeline workflows pass |
| SEC-02 | P0 | Free-form subject metadata can become part of a training output path. | Preserve display metadata separately; create a filesystem-safe slug plus stable short hash, reject separators/control/dot segments/reserved names, and verify the resolved output remains under the authorized root. | verified at checkpoint `c7e3717e`: display metadata is preserved separately, output identities use normalized slug plus SHA-256 prefix, unsafe/reserved values fail before generation, multi-subject generation is transactional, and POSIX writes remain bound to a no-follow directory identity |
| SEC-03 | P1 | RAG can initialize an unpinned embedding model without first-run consent, quota preflight or offline-only runtime. Vector data is stored under the package tree. | Pin `sentence-transformers/all-MiniLM-L6-v2@1110a243fdf4706b3f48f1d95db1a4f5529b4d41`, use the Granite consent/cache policy, load only the verified contained local snapshot with remote code disabled, and store vectors in the user data/cache directory. Missing cache disables RAG without breaking Assistant. | verified at checkpoint `28f2d875` and re-gated in the working checkpoint: pinned embedding revision, consent/quota preflight, verified absolute local snapshot, remote-code denial and user-cache storage with non-fatal unavailable state; the real offline MiniLM/Qdrant gate passes all 10 checks and 3 known-query cases, while 122 adjacent RAG/core/script tests pass |
| SEC-04 | P1 | Local model loading has no final RAM/VRAM admission despite catalog estimates. | Run centralized resource admission before `from_pretrained`; blocking results prevent load, OOM returns a recoverable UI state and releases cache. | verified at checkpoint `1ce864a5`: every activation performs selected-device admission before `from_pretrained`; stale first-run consent cannot bypass warning/unknown/blocking risk, CUDA OOM releases partial resources, and close-during-load is bounded |
| SEC-05 | P1 | Assistant Markdown link activation can pass arbitrary URL schemes, including local-file-like targets. | Allow HTTPS with host confirmation. File artifacts require a typed host-created action. Reject `file:`, `data:`, `javascript:` and custom schemes. | verified at checkpoint `1068f012`: HTTPS uses host confirmation and unsafe/custom/local schemes are rejected |
| SEC-06 | P1 | Lexical path validation may be bypassed by POSIX symlinks or Windows junction/reparse points. | Resolve final identity consistently and reject or containment-check links/reparse targets under the selected root. | verified for the code-controllable boundary in the working checkpoint: Assistant file inspection requires a host-created filesystem-identity grant; training/evaluation manifests, NPZ payloads and checkpoints use retained-directory, descriptor-relative POSIX IO with no-follow/exclusive creation and reject symlink, hardlink and non-regular leaf substitutions; the Windows leaf opener uses native reparse-point handles and link-count validation; legacy direct `load_data` is denied before ApplicationService because reopening scanned child strings cannot preserve that identity. The 52-test focused persistence/path suite passes. Native Windows/NTFS junction and leaf-reparse behavior remains a human platform acceptance boundary under FUNC-06. |
| SEC-07 | P1 | Logs and exception paths can expose complete private EEG paths and subject identifiers. | Add centralized redaction, basename/hash path display, opt-in detailed diagnostics, retention and permissions policy. | verified for the code-controllable boundary: centralized public-by-default redaction, bounded traceback/record/file retention, legacy-log sanitization and owner-only storage pass the main-agent 283-test logger/privacy/ACL re-gate. Native Windows read-back independently confirms a protected current-user-only DACL on the directory, active log and rotated files; ACL/API verification failures disable the file sink. Packaged-launcher, non-NTFS and second-account denial remain explicit Windows acceptance boundaries rather than product-complete claims. |
| SEC-08 | P2 | Directory enumeration and model downloads do not have complete bounded-consumption controls. | Use bounded `os.scandir`, deadline/cancel/truncation, staged downloads, streaming quota/free-space checks and partial cleanup. | verified in the working checkpoint: cache scans enforce deadline/cancellation plus 100,000-entry/64-level limits; RAG downloads use a 15-minute deadline, isolated staging, live quota checks, bounded child cleanup and a cross-process publication lock; the main-agent 116-test RAG/core slice passes. Native Windows lock/reparse behavior and live-network interruption remain platform acceptance boundaries, not silent product claims |
| SEC-09 | P1 | Product model choices retain legacy Phi entries, including a `trust_remote_code` path. | Product selection becomes exact Granite 3.3 2B only. Existing protected settings receive a migration message; no silent fallback and no automatic rewrite. | verified at checkpoint `28f2d875`: product catalog and runtime are exact Granite 3.3 2B only, with explicit legacy-setting migration messaging |
| SEC-10 | P1 | RAG/context data are not consistently encoded as untrusted data. | Use bounded structured data with source labels; strip control characters/private paths and keep data outside instruction/policy fields. Add injection regressions. | verified in the working checkpoint: bounded untrusted-data envelopes, source labels and policy separation are enforced; POSIX, Windows and UNC paths with spaces are fully redacted while surrounding prose is preserved. The main-agent public-diagnostic/context/assembler re-gate passes 330 tests; final exact-SHA closure remains under FUNC-06 |
| SEC-11 | P1 | Resource-calibration validation compares the source digest but can accept an artifact recorded from another branch/commit or a dirty tree. | Exact-source calibration checks branch, full commit, clean state and source/tree identity before a handoff PASS; stale tracked calibration remains checkpoint-only. | verified in the working checkpoint: exact validation compares branch, full commit, tree, unprotected dirty state, protected local settings and source digest; the canonical manifest writes the calibration to the ignored full-SHA dossier and preserves it as dashboard input, while the tracked default reports checkpoint-only; 91 main-agent resource/dashboard tests pass |

## Functional Correctness and Validation

| ID | Severity | Finding and evidence | Required closure | Status |
| --- | --- | --- | --- | --- |
| FUNC-01 | P0 | Stop can interrupt after an optimizer step without advancing the current holder. A later Start clears the interrupt and can resume the cancelled plan before the new plan. | Define Stop as terminal cancel. Mark the admitted plan's current/remaining holders cancelled and advance the queue. Resume is not implicit. | verified at checkpoint `ae441050`: Stop terminally cancels the admitted plan and advances remaining holders; Start cannot implicitly resume it |
| FUNC-02 | P1 | Training plan IDs use second resolution and output directories use `exist_ok=True`, allowing collisions and stale result reuse. | Use timestamp plus UUID and exclusive directory creation. No implicit resume without a future complete manifest/state protocol. | verified across checkpoints `ae441050` and `c7e3717e`: plan IDs use microseconds plus UUID, output leaves are exclusive, and existing/legacy runs are rejected instead of resumed |
| FUNC-03 | P1 | The nominal full-pipeline test constructs epochs/splits directly and mocks persistence, so it does not prove the user workflow it claims. | Rename the existing scope and add a real ApplicationService FIF import-to-visualization smoke without persistence mocking. | verified at checkpoint `316c9e81`: the direct trainer suite now states its limited scope, while a separate real command-spine smoke executes FIF import through persisted EEGNet training, evaluation and visualization readiness without persistence mocks |
| FUNC-04 | P1 | The dashboard can pass while required public fixtures are skipped. | Handoff/release profile validates a fixture manifest first and fails on skip, xfail or deselection. | verified: the required-ci fixture definition is pinned by a canonical manifest digest, every handoff run revalidates the git-ignored cache, and skip / xfail / xpass / deselection are hard failures; main-agent reruns passed 43 focused tests, the 14.8 MB cache verification, and the strict 20-case / 14-format dataset matrix |
| FUNC-05 | P1 | Execution smokes do not prove event-to-label correctness, split disjointness or held-out predictions. | Add a deterministic oracle dataset with explicit semantic assertions and finite output checks. | verified at checkpoint `a0729a07`: the deterministic oracle asserts source event-to-class semantics, exact disjoint/exhaustive splits, held-out target/logit alignment, finite metrics and probabilities, and real artifact persistence without making a scientific-accuracy claim |
| FUNC-06 | P1 | Current UI/Granite evidence is not consistently bound to the clean candidate commit and some screenshots disagree with current source. | Regenerate exact-commit artifacts containing commit, dirty state, environment, generator, claims and limitations. | in progress: strict Granite evidence now seals requested/loaded model, pinned revision, actual cache-snapshot content, source identity, host assistance, screenshot bytes and terminal shutdown; dashboard evidence records before/after source identity and rejects branch, commit, dirty-fingerprint or source-tree drift. The canonical manifest supplies `build/handoff-evidence/<full-SHA>` explicitly to every final artifact generator and records capture identity, environment, viewport/scale, screenshot hashes, claims and limitations. Shared redaction removes model/RAG cache paths in POSIX, drive-letter, JSON-escaped and WSL UNC forms from logs, artifacts and dossiers before persistence, with validation rejecting reintroduction. The main-agent integrity/privacy re-gates pass their focused suites and two independent reviewers report PASS. Final artifacts must still be regenerated from one clean pushed exact commit. |
| FUNC-07 | P1 | Stop/restart lacks a barrier test at the vulnerable optimizer-step boundary. | Add a deterministic barrier regression and verify only the new plan runs after Stop. | verified at checkpoint `09aa2a3b`: the deterministic real optimizer-step barrier proves the active old step may finish, all old current/queued holders remain cancelled, no queued old optimizer step runs, and only the fresh plan executes after restart |
| FUNC-08 | P2 | Sleep-EDF and CHB-MIT sidecars are raw-only evidence but can be overread as label support. | Keep explicit capability boundaries in reports and documentation. | verified: the strict 277,106,963-byte teacher-preflight profile classifies CHB-MIT seizure and Sleep-EDF hypnogram sidecars as unsupported/limited context, imports only raw EEG, keeps `supervised_ready=false`, and explicitly disclaims automatic sidecar-label and full-BIDS support |
| FUNC-09 | P1 | The canonical handoff manifest omits the complete regression runner and exact-Granite recovery/long-session evidence, so a dossier can be structurally complete while required behavior was never run. | Register the complete regression, exact Granite recovery and bounded exact Granite long-session commands with exact environment, artifact and ordering contracts; keep dashboard last. | verified in the working checkpoint: the registry includes the isolated complete regression, exact-Granite recovery and bounded 600-second long-session capture, then exact-source resource calibration and dashboard-last preservation; entrypoint/order/environment/artifact contracts are guarded and the main-agent manifest/evidence suite passes 150 tests. The commands themselves still require the final clean-SHA run under AGENT-04/FUNC-06. |
| FUNC-10 | P1 | Several dashboard pytest-like checks validate only process return code and can pass with skip, xfail, xpass, deselection, errors or no pytest summary. | Required/handoff pytest call sites use an explicit strict outcome policy and tests prove every forbidden outcome fails. | verified in the working checkpoint: mandatory dashboard pytest checks require a real summary and reject failed, errors, skipped, xfailed, xpassed and deselected outcomes; the broad non-denominator IO checkpoint remains explicitly relaxed, while canonical mandatory public IO stays strict; 91 main-agent dashboard/resource tests pass |

## Assistant and Agent

| ID | Severity | Finding and evidence | Required closure | Status |
| --- | --- | --- | --- | --- |
| AGENT-01 | P0 | Native generation timeout was published only after the worker exited, while the local backend performed an unbounded join. UI could remain `Stopping` forever. | Run Granite in a supervised subprocess. Allow a short cooperative cancel grace, terminate the worker on timeout, fence the turn, show recoverable restart-required state and bound application close. | verified in Linux/WSL: spawn-owned runtime, 750 ms cooperative grace, owned-process escalation, correlated fencing, explicit Retry and bounded close; all 1,960 LLM unit tests pass. Native Windows/frozen CUDA-hang acceptance remains a platform claim boundary |
| AGENT-02 | P1 | Endpoint selection can depend on set/hash order when multiple workflow endpoints share a rank. | Choose highest workflow order, then last textual mention; ask for clarification when intent remains contradictory. Prove across hash seeds and word orders. | verified at checkpoint `3037dc0b`: equal-rank endpoints use the last textual mention, explicit unresolved choices ask for clarification, and 505 adjacent intent/scope/admission/controller tests pass across multiple hash seeds |
| AGENT-03 | P1 | Conversation context keeps only a small visible suffix and transcript growth eventually hard-blocks. | Add turn-bound archive/pruning, explicit unresolved-reference clarification and a long-session soak with external GUI state changes. | verified in the working checkpoint: a 202-turn product/controller/UI soak crosses two pruning cycles, external ApplicationService load/reset state changes, resize pressure, unresolved historical-reference clarification and persisted typed-history restore without hard block; main-agent rerun passed in 88.11 seconds. This is deterministic product evidence, not real-Granite accuracy evidence |
| AGENT-04 | P1 | Product evidence for Granite is a narrow host-assisted workflow and is stale relative to the clean source. | Re-run exact Granite, secure offline RAG, confirmation/error/retry/cancel and long-session product evidence. Do not call it thesis accuracy. | open |
| AGENT-05 | P2 | Text glyphs are used for toolbar actions and have inconsistent accessibility/DPI behavior. | Use standard icons, tooltips, accessible names and narrow/high-DPI validation. | verified as a working checkpoint: the Assistant title bar uses Qt/repository icons rather than text glyphs, with consistent 30 px controls, 16 px icons, tooltips and accessible names/descriptions. The visible status badge was removed; normalized runtime state remains available through typed panel state, tooltip and accessibility metadata. The focused chat/bubble/toolbar suite passes 136 tests and the 100/125/150% offscreen DPI gate passes. Exact-source full-window and Windows native DPI evidence remains under UI-09. |
| AGENT-06 | P2 | Turn orchestration, tool attempt lifecycle, process ownership and Qt presentation are concentrated in large controller/manager classes. | Extract `TurnOrchestrator`, `ToolAttemptSession` and `RuntimeProcessOwner`; keep AgentManager as Qt adapter. | verified in the working checkpoint: `AssistantTurnOrchestrator` owns host/RAG/generation/cancellation correlation, `AssistantToolAttemptSession` owns request-scoped counters and repeated-proposal history, `ProcessRAGRetrieverLifecycle` owns the RAG subprocess, and Qt runtime/dispatch/publication coordinators keep `AgentManager` at the composition/presentation boundary. All 25 writable controller compatibility aliases and coordinator-local turn history are removed. An AST repository guard rejects controller/test reintroduction while allowing worker-owned generation state. Main-agent validation passes 334 unit/architecture plus 208 integration/UI tests, the checker reports zero aliases, Ruff/BasedPyright are clean for the slice, and the independent ownership re-gate reports PASS. Exact-source product evidence remains under AGENT-04/FUNC-06/UI-09. |

## UI and Product Experience

| ID | Severity | Finding and evidence | Required closure | Status |
| --- | --- | --- | --- | --- |
| UI-01 | P1 | Opening Assistant can shrink Dataset content to an unusable width while the fixed aggregate sidebar remains visible. | Preserve the previously accepted fixed right-side `Data Summary` table across all five panels. Bound the Assistant and main content responsively so neither overlaps or displaces that sidebar; do not move the summary into a tab or drawer. | verified as a working checkpoint: the prior compact 13-row `Data Summary` table is restored at the same right-side position across Dataset, Preprocess, Training, Evaluation and Visualization. The focused information-panel/geometry gate passes; current narrow-shell captures confirm complete labels. Exact-source full-window/DPI artifacts remain required under UI-09. |
| UI-02 | P1 | Disabled Start Training did not explain the first blocking prerequisite. A persistent Readiness block then consumed scarce sidebar space. | Do not add a dedicated visible Readiness section. Surface the first blocking reason and direct next action through the existing status/action presentation while keeping Configuration and Execution controls stable. | in progress: the persistent Readiness block is removed and the compact fixed sidebar is restored. Final exact-source loaded/blocked/training artifacts must still prove that the existing status/action presentation explains the next step without crowding the sidebar. |
| UI-03 | P1 | Match Labels exposes internal `Role` terminology beside user-facing `Use as`. | First layer uses task language only; internal role/evidence moves to report/advanced. Recipe navigation says `Go to Match Labels`. | in progress: first-layer task language, advanced evidence and `Go to Match Labels` navigation pass the main-agent 183-test focused UI suite; exact-source Match Labels artifacts remain required under UI-09 |
| UI-04 | P2 | Post-import next action is only visible through low-salience status feedback. | Show one contextual `Continue to Preprocess` or `Create epochs` action after successful import. | verified as a working checkpoint: Dataset shows one contextual post-import action and advances it from `Continue to Preprocess` to `Create EEG epochs` or `Configure Training` as application state changes; the main-agent Dataset panel re-gate passes 40 tests. Exact-source full-window evidence remains under UI-09. |
| UI-05 | P2 | Preprocess plots do not explain raw/processed/event/excluded visual encodings. | Add a compact legend without resizing the stable preview area. | verified as a working checkpoint: the stable preview includes a compact loaded/current/event/excluded legend and keeps presentation states covered; the main-agent adjacent presentation re-gate for UI-05 through UI-07 passes 69 tests. Exact-source full-window evidence remains under UI-09. |
| UI-06 | P2 | Visualization lacks persistent dataset/model/fold/run/grouping provenance. | Add a compact provenance bar or details action without restoring the removed verbose fixed row. | verified as a working checkpoint: Visualization shows compact dataset, fold, run, grouping and aggregation provenance above the plot tabs without restoring the removed verbose row; the main-agent adjacent presentation re-gate for UI-05 through UI-07 passes 69 tests. Exact-source full-window evidence remains under UI-09. |
| UI-07 | P2 | A redesign of Data Summary hid absent rows and changed the established sidebar vocabulary, making the panel move and scan differently across workflow stages. | Restore the previously accepted compact `Data Summary` table, stable 13-row order and fixed right-sidebar placement. Missing values render as `-`; modern detached publication data remains the only state source. | verified as a working checkpoint: the old visual table and concise labels are restored without restoring backend queries or live-object reads. The focused information-panel/geometry gate passes and current five-panel baseline artifacts show a stable sidebar; exact-source loaded/DPI evidence remains under UI-09. |
| UI-08 | P2 | Training epoch and EEG epoch language is ambiguous outside the established aggregate table. | Preserve the accepted concise labels inside `Data Summary`, while using explicit `EEG epoch` versus `training epoch` terminology in workflow actions, dialogs, reports and Assistant guidance. | verified as a working checkpoint: product actions, dialogs, reports and Assistant guidance distinguish `EEG epoch` from `training epoch`; the accepted concise `Data Summary` labels remain unchanged. The same-class source guard and focused 277-test regression pass. Exact-source product evidence remains under UI-09. |
| UI-09 | P1 | Existing screenshots do not consistently prove narrow layout, DPI, Assistant dock and dialog geometry against exact source. | Capture full-window artifacts at required sizes and 100/125/150% scaling; main agent inspects every required image. | in progress: current-source Assistant captures cover loading, empty, conversation, confirmation, long content, 320/760/1280 widths and 100/125/150% offscreen scale; current-source Data Import replay covers preview, remap and applied states. The main agent inspected the focused images and found no clipping, overlap or transcript-wide horizontal overflow. The seven approved shell/panel baselines also pass two-consecutive-frame stability and match their references exactly, including the fixed legacy `Data Summary` and absent visible Readiness block. These remain dirty-checkpoint artifacts under `build/dev-artifacts/`; clean exact-SHA regeneration and Windows native DPI remain required. |
| UI-10 | P2 | Product surfaces have inconsistent hierarchy, disabled/destructive actions, empty states and table/header/selection styling. | Introduce scoped shared product components/tokens and perform a same-class sweep; do not globally rewrite the theme. | verified as a working checkpoint: generic, history, metrics, combo-popup and relevant tree selections use one scoped low-key `Theme.TABLE_SELECTION` token while stronger blue remains reserved for primary/pressed actions. The focused same-class suite passes 240 tests, Ruff is clean, and the independent UI product re-gate reports PASS with no P0/P1 finding. Clean exact-source artifacts remain required under UI-09. |

## Documentation and Repository Hygiene

| ID | Severity | Finding and evidence | Required closure | Status |
| --- | --- | --- | --- | --- |
| DOC-01 | P1 | Branch/handoff instructions disagree about the active candidate and stabilization line. | Publish one current integration/handoff rule and update all canonical entry points. | verified: Current, Now, Validation, the agent stack and handoff workflow identify `stabilize/product-quality-closure` as the active integration line and the prior UX commit as provenance baseline only |
| DOC-02 | P1 | Homepage/current/now disagree on whether the candidate is closed, awaiting re-audit or handoff-ready. | Separate automated candidate truth from Windows acceptance and use the same wording everywhere. | verified: homepage, Current, Now and Validation consistently classify this branch as an in-progress checkpoint; automated closure creates only a Windows handoff candidate and human acceptance remains separate |
| DOC-03 | P1 | Docs claim one worktree while Git has three; branch inventory and test totals are stale or contradictory. | Generate inventory/evidence from exact commit and avoid manually duplicated totals. | verified for canonical docs: registered worktree inventory is command-generated instead of copied into current truth, test totals are not manually aggregated, and final totals require exact-commit generated evidence; final artifact generation remains under FUNC-06 |
| DOC-04 | P1 | `docs/validation/README.md` mixes current truth with dated history and contradictory static-analysis results. | Keep concise current gates and move dated evidence to records. | verified: Validation now contains the current evidence contract, one handoff command manifest and claim boundaries; dated checkpoint output remains in records/generated artifacts |
| DOC-05 | P2 | Active queue and old goal documents still dispatch completed/MCP/legacy work. | Retain one active goal; remove stale current goal files after integrating useful constraints. Git history remains the archive. | verified: one active product-quality goal remains, the old queue and six obsolete goals are removed, and retired queue names appear only in explicit do-not-use/history language |
| DOC-06 | P2 | Current artifacts contain duplicate screenshot groups and an outdated index. | Keep one canonical artifact per state/viewport, regenerate the index and record provenance. | in progress: artifact governance distinguishes tracked checkpoints from ignored clean-SHA dossiers; 449 superseded tracked screenshots were intentionally removed after reference review, while current UI baselines and documented checkpoint families remain. Final closure still requires the exact-SHA dossier and a final reference/index check from the clean commit. |
| DOC-07 | P2 | MkDocs navigation surfaces huge worklog/implementation history while hiding decision-oriented content. | Keep current/architecture/validation prominent and move logs to a records/history area. | verified: current truth, decisions, planning, architecture and validation are first-class navigation; implementation/work logs are nested under Records |
| DOC-08 | P1 | Repo-local skills still mention BackendFacade/MCP as active architecture in places. | Sweep `.agents` instructions and align them with the current ApplicationService/local-only/MCP-retired truth. | verified: repo-local guidance uses ApplicationService/Command API, treats BackendFacade as physically removed and MCP as opt-in retired scope; source search and `mkdocs build --strict` pass |

## Automated Exit Conditions

The active goal cannot finish while any code-controllable P0/P1 item is open.
It also requires:

1. focused regression and same-class/source guards for each repaired class;
2. real ApplicationService happy path plus deterministic oracle coverage;
3. strict fixture manifest and multi-dataset gates with zero hidden skips;
4. exact Granite plus secure offline RAG walkthrough;
5. reviewed UI artifacts at narrow/full/DPI layouts;
6. Ruff, full Basedpyright, architecture, pytest, MkDocs and quality dashboard;
7. clean and pushed integration branch with protected settings untouched.

Passing these conditions creates a Windows handoff candidate. Product completion
and merge to `main` still require user acceptance.
